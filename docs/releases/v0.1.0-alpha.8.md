# Release draft: `v0.1.0-alpha.8` — the network on the chip

Drafted for the principal's stamp at the end of PR F (ISA round 33):
three actions, post-merge, in this order — the tag, the Gitea release
with the one binary, `cargo publish`, the last with two checks before
it that alpha.7 lacked (the packed tree scanned before the upload, and
the grep of ROADMAP § 0.1.0 check 13). This file is the notes body, the
file table and the procedure; the binary is not in git, and its row in
§ Files is filled at the stamp.

## Procedure

| Step | Command / field | Value |
|---|---|---|
| 1. Tag | `git tag -a v0.1.0-alpha.8 <merge commit> -m "neuralos-snn 0.1.0-alpha.8"` then `git push origin v0.1.0-alpha.8` | the mirror procedure (AGENTS.md § Merge procedure) carries it to GitHub; the two tag lists identical |
| 2. Release (one API call on Gitea) | Tag | `v0.1.0-alpha.8` (existing) |
| | Title | 0.1.0-alpha.8 — the network on the chip |
| | Body | § Notes below, verbatim |
| | Assets | the file in § Files, plus its `SHA256SUMS`, both in `firmware/esp32c3/dist/` (gitignored), produced by the evidence README § Release asset: `firmware/esp32c3/build.sh` at the merge commit, its `.text` equal to the seventh entry's pin; both downloaded back, `sha256sum -c` clean, the remap gate on the download 0 hits |
| | Pre-release | yes (alpha) |
| 3. Publish | the dry-run, at the merge commit with a clean tree | `cargo publish -p neuralos-snn --dry-run`, then `cargo package -p neuralos-snn` for the `.crate` at `target/package/` (block below) |
| | the packed tree, BEFORE the upload | `target/package/neuralos-snn-0.1.0-alpha.8.crate` unpacked into `target/package/scan/` (in the repo, gitignored under `target/`; never `/tmp`), then three greps: the tracked-tree job's pattern, home paths, session links. 0 hits, or no publish |
| | the check-13 grep | over the three release texts: this file, the crate README, the root README. 0 hits, or no publish |
| | the publish | `cargo publish -p neuralos-snn`, then the registry read back: `created_at`, the checksum equal to the local `target/package/neuralos-snn-0.1.0-alpha.8.crate`, `rust_version` 1.92, `max_version` |

Step 3, from the repo root at the merge commit. On cargo 1.98.1 the
dry-run packs into `target/package/tmp-crate/`, not into
`target/package/` where alpha.7's cargo (1.92.0) left the file;
`cargo package` writes the same bytes to that path (one sha, measured
at PR F's head).

```bash
cargo publish -p neuralos-snn --dry-run
cargo package -p neuralos-snn             # the .crate at target/package/, the dry-run's bytes
mkdir -p target/package/scan
tar -xzf target/package/neuralos-snn-0.1.0-alpha.8.crate -C target/package/scan
grep -rn -E -i "$(cat .githooks/personal-pattern)" target/package/scan/   # the tracked-tree job's pattern: 0 hits
grep -rn '/hom[e]/' target/package/scan/                                  # home paths: 0 hits
grep -rn -i -E 'claude\.ai|claude-session' target/package/scan/           # session links: 0 hits
# check 13 (ROADMAP § 0.1.0); the bracketed letters keep this line from matching itself
rg -n -i 'golde[n]|conformanc[e]|successo[r]|lav[a]' docs/releases/v0.1.0-alpha.8.md crates/neuralos-snn/README.md README.md
cargo publish -p neuralos-snn
```

## Stamped

Filled at the stamp. The record of the three actions rides the next
PR's first commit, with the ISA head line and the RESEARCH_LOG
paragraph (the alpha.8 rule, decided 2026-09-12: no separate close-out
PR).

## Files

| Asset | SHA-256 | Size (B) | What it is |
|---|---|---|---|
| `neuralos-esp32c3-0.1.0-snn-0.1.0-alpha.8-riscv32imc-unknown-none-elf.elf` | filled at the stamp | filled at the stamp | the firmware of `evidence/esp32c3-bringup/board-r33-alpha8.log`'s sources, built by `firmware/esp32c3/build.sh` at the tagged commit (the path remap, the personal-string gate 0 hits, no scrub; `SOURCE_DATE_EPOCH` the commit's time); its `.text` must equal the seventh entry's pin, `6f8ec5385617a1345d251591d237a77718eed1952686d7ab50a5ca0962a06a77` (evidence README § Rebuild + run), checked at the build |
| `SHA256SUMS` | | | the line above; `sha256sum -c SHA256SUMS` passes from `firmware/esp32c3/dist/` or from a download directory holding both files |

Versions inside: firmware crate 0.1.0 · library `neuralos-snn`
0.1.0-alpha.8, the version this release publishes to crates.io.

## Notes (the release body)

**`neuralos-snn` 0.1.0-alpha.8** — the `no_std`, i16 fixed-point
spiking-neuron library for RISC-V edge silicon. The network on the
chip.

Since alpha.7:

- **`FixedNetwork<N, S>`**, `no_std`: `N` neurons and `S` synapses in
  arrays, no heap, no plasticity, the std network's step in the same
  order; its step has no `Result`, no index and no division of its
  own. `FixedNetwork::try_from(&net)` converts a plasticity-off
  `SpikingNeuralNetwork` of the same size on the host. **No panic path
  in its step, proven at link time** (`proofs/no-panic-step/`: a panic
  handler that is an undefined symbol, under the firmware's profile
  and under one without cross-crate LTO; the one path the proof
  found, a division in `dt_over_tau`'s wide half, now divides by a
  `NonZero`, behavior identical).
- **Traces:** `tests/traces/`, thirteen cases of the network as text
  (`neuralos-trace v1`: the spikes and every membrane, step by step),
  compared by `cargo test` against the code; a trace that moves is
  red at its line. A reference vector pins the bare neuron against an
  `i128` model on 150 rows.
- **The spike ring leaves the neuron.** `LIFNeuron` keeps no history;
  `SpikeRecorder`, a `no_std` 64-entry ring the caller feeds from
  `integrate_and_fire`'s return, holds it, with the crate-private
  firing-rate and ISI readers. `size_of::<LIFNeuron>()` is 44 bytes
  (320 on alpha.7), pinned by a test on the host (x86-64); the same
  44 on riscv32imc was read at PR D (#28), not asserted in the tree.
  API: the one public path that moved is `MAX_SPIKE_HISTORY`, at the
  crate root and in `spike_recorder`, no longer under `lif_neuron`;
  the neuron's history readers were crate-private and stay so, on the
  recorder.
- **The MSRV is tested.** `rust-version = "1.92"`, checked on 1.92.0
  in CI in three configurations (`std`, `no_std`, `simd`); the
  toolchain pin is 1.98.1.

**Measured on the ESP32-C3 (rv32imc, 160 MHz), the same SuperMini:**
every plasticity-off trace of `tests/traces/`, twelve of the thirteen,
replays on the chip bit for bit from the arrays the host steps, the
spikes and every membrane, row for row (`plasticity-on` is outside
`FixedNetwork` by design). The frozen `feedforward-8` (8 neurons, 6
synapses) steps in **14,058 ns per step of the whole network**, its
spike tally included; the neuron as a network holds it 1,576 ns/step
(1,579 on alpha.7). Every behavior figure of alpha.7 is unchanged:
147 spikes in the 10,000-step burst, the first at step 55, the same
checksum. Numbers, logs and `.text` pins:
`evidence/esp32c3-bringup/README.md` § Sixth and § Seventh entry. On
x86-64 the host spike-path bench is within a nanosecond per step of
alpha.7 on the same compiler (forced −0.6, control +0.05 ns/step, one
run): § Host bench, round 33 of the same README. The same twelve traces
replay bit for bit under QEMU `riscv64gc` (`proofs/qemu-trace-replay/`,
`evidence/qemu-riscv-gate/`).

**The attached binary** is the firmware of the seventh entry, built at
the tagged commit by `firmware/esp32c3/build.sh` (no home path in it
by construction; sha in `SHA256SUMS`, the `.text` pin in the evidence
README). Rebuild from source by the evidence README § Rebuild + run;
compare the `.text` section, not the file. A version bump alone moves
the `.text` (the symbol hashes follow the version); the behavior
figures are the comparable across versions.

**The road to 0.1.0** is `docs/ROADMAP.md` § 0.1.0: fourteen checks a
stranger can run, six of them holding at this release.

What this release is not: not a benchmark (one board, one network
shape), not a network with plasticity on the chip, not a change to any
published number of the paper.

Source: this repository at the tagged commit. License AGPL-3.0.
