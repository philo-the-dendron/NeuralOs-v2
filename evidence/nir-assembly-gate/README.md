# NIR assembly gate — general four-kind graphs (R18, 2026-08-22)

The evidence gate of the NIR general-assembly slice: any
reference-emitted Input/Linear/LIF/Output graph assembles and fires.

Reference: `neuromorphs/NIR` @ `7883c3c85f1be27ed113ccc9e8d6ab47ab541df4`
(clone at `nir-ref/`, gitignored). Fixtures regenerated by
`.nirenv/bin/python3 tools/gen_nir_fixtures.py` — every graph
constructed with explicit Input/Output nodes; emissions self-assert
their node/edge sets (the `infer_types` auto-wire trap).

## Rebuild

```bash
cargo run -p neuralos-snn --example nir_assembly_gate   # 6/6 gates
```

## The 6 gates (gate.log is the run of record)

1. branch: 4 neurons all fire; first-spike steps pinned `[3, 14, 1, 3]`
   (hand-verified integer dynamics, mV, drive x=[6,0,0]);
2. merge: single branch stalls (0 spikes/200), summed fan-in fires —
   neuron 0 first spike step 52 exactly (g: 1620→116 by g/20);
3. recurrent: D1's assert — b0 at exactly −6990 one step after a0's
   spike (+10 centi-quanta from the 20 μA EDGE_PULSE_QUANTA pulse);
4. fusion: the l1·l2 stage IS the once-quantized f64 product
   `quantize_linear(W₂·W₁)`;
5. 10 named rejections, one negative fixture each (incl. Input→LIF
   direct drive and recurrent-on-mV with the copy-pasteable remedy);
6. the frozen chain re-runs UNCHANGED through both builders:
   9 spikes/100 @ step 6, export 826 B, general-builder raster
   bit-identical.

## Honest-claim language (the language of record)

Structure, per-edge type shapes, and quantization records are exact
w.r.t. the pinned document; dynamics are named substrate conventions
(`EDGE_PULSE_QUANTA = 200`, synaptic divisor 10, voltage grid,
one-step synaptic delay, frozen plasticity) pinned by exact-value
tests; the reference defines no execution semantics at this sha — no
numeric-parity claim is made or implied.

Companion frozen-pin re-verification this session: `nir_format_gate`
4/4 (826 B export) and `nir_hdf5_gate` 5/5 (941 B JSON export) —
byte-identical to their banked records.
