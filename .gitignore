# Rust
/target/
**/target/
**/*.rs.bk
# Cargo.lock IS committed (workspace has binaries; the hdf5-sys vendored-C
# pin must be deterministic; rust-cache keys off the lockfile).
*.swp
*.swo
*~
.vscode/
.idea/
*.iml
.DS_Store

# Tauri (future app)
dist/
node_modules/

# OS
Thumbs.db

# Editor
*.sublime-*

# Stage-4 model weights — downloaded, never committed
models/

# Session/harness tooling locals (autopsy R1)
.opencode/

# Foreign fork built by tools/build_fork.sh — never committed
fork-build/

# Benchmark rebuild scratch (evidence/simd-hardening/README.md) — never committed
bench-rebuild/

# Paper figure venv (repo-local alternative to system python)
.figvenv/

# NIR reference clone (pinned 7883c3c, fixture generation only)
nir-ref/

# NIR python env (numpy/h5py/cmake for fixture generation + the vendored
# HDF5 cmake binary; rebuilt: python3 -m venv .nirenv && .nirenv/bin/pip
# install numpy h5py cmake ./nir-ref)
.nirenv/

# HDF5 plugin dir (runtime HDF5_PLUGIN_PATH target; empty until a plugin ships)
tools/hdf5-plugins/*
!tools/hdf5-plugins/.gitkeep
